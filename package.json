{
  "name": "kinbook",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/cli build && bun run --cwd packages/mcp-server build && bun run --cwd packages/web build",
    "gen:db": "bun run --cwd packages/database gen"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}